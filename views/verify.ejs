<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <title><%= appName %> — Verify OTP</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="/style.css" />
  </head>
  <body>
    <div class="app app-centered">
      <header class="app-header">
        <h1><%= appName %></h1>
        <p>We have sent an OTP to your email/phone (check server console).</p>
        <button type="button" id="theme-toggle" class="secondary small">
          Toggle theme
        </button>
      </header>

      <main class="app-main auth-main">
        <section class="panel auth-panel">
          <h2>Verify OTP</h2>

          <% if (error) { %>
          <div class="auth-error"><%= error %></div>
          <% } %>

          <form method="POST" action="/verify" class="auth-form">
            <input
              type="hidden"
              name="identifier"
              value="<%= identifier || '' %>"
            />

            <div class="form-group">
              <label>Account</label>
              <input
                type="text"
                value="<%= identifier || '' %>"
                disabled
              />
            </div>

            <div class="form-group">
              <label for="otp">OTP Code</label>
              <input
                required
                type="text"
                id="otp"
                name="otp"
                placeholder="Enter 6-digit OTP"
              />
            </div>

            <div class="form-actions">
              <button type="submit" id="verify-btn">Verify</button>
            </div>
          </form>

          <div class="auth-links">
            <p class="muted">
              Already verified?
              <a href="/login">Go to login</a>
            </p>
          </div>
        </section>
      </main>
    </div>

    <script src="/theme.js"></script>
  </body>
</html>